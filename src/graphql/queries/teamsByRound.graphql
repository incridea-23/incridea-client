query TeamsByRound(
  $eventId: ID!
  $roundNo: Int!
  $after: String
  $first: Int
  $contains: String
) {
  teamsByRound(
    eventId: $eventId
    roundNo: $roundNo
    after: $after
    first: $first
    contains: $contains
  ) {
    edges {
      node {
        attended
        id
        name
      }
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
