query TeamDetails($id: ID!) {
  teamDetails(id: $id) {
    ... on QueryTeamDetailsSuccess {
      __typename
      data {
        attended
        confirmed
        id
        members {
          user {
            createdAt
            email
            id
            isVerified
            name
            phoneNumber
            role
          }
        }
        name
      }
    }
    ... on Error {
      __typename
      message
    }
  }
}
