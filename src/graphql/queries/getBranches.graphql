query Branches {
  getBranches {
    id
    name
    branchReps {
      branchId
      userId
      user {
        email
        id
        isVerified
        name
        phoneNumber
        role
      }
    }
  }
}
