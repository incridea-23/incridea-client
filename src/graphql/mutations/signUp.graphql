mutation SignUp(
  $email: String!
  $collegeId: Int!
  $name: String!
  $password: String!
  $phoneNumber: String!
) {
  signUp(
    data: {
      collegeId: $collegeId
      email: $email
      name: $name
      password: $password
      phoneNumber: $phoneNumber
    }
  ) {
    ... on Error {
      __typename
      message
    }
    ... on MutationSignUpSuccess {
      __typename
    }
  }
}
