mutation CreateEvent($name: String!, $eventType: EventType!) {
  createEvent(data: { name: $name, eventType: $eventType }) {
    ... on Error {
      __typename
      message
    }
    ... on MutationCreateEventSuccess {
      __typename
    }
  }
}
