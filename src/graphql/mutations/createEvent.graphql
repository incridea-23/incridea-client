mutation CreateEvent($name: String!) {
  createEvent(data: { name: $name }) {
    ... on Error {
      __typename
      message
    }
    ... on MutationCreateEventSuccess {
      __typename
    }
  }
}
