query MyQuery($eventId: ID!) {
  myTeam(eventId: $eventId) {
    ... on Error {
      __typename
      message
    }
    ... on QueryMyTeamSuccess {
      __typename
      data {
        attended
        confirmed
        id
        leaderId
        name
        members {
          user {
            email
            id
            phoneNumber
            name
            college {
              name
            }
          }
        }
      }
    }
  }
}
