mutation CreateCollege($details: String!, $name1: String!) {
  createCollege(name: $name1, details: $details) {
    ... on Error {
      __typename
      message
    }
    ... on MutationCreateCollegeSuccess {
      __typename
      data {
        id
        name
      }
    }
  }
}
