mutation DeleteHotel($id: String!) {
  deleteHotel(id: $id) {
    ... on Error {
      __typename
      message
    }
    ... on MutationDeleteHotelSuccess {
      __typename
      data {
        details
        id
        name
        price
      }
    }
  }
}